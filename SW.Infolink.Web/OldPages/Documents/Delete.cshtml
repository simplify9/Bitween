@page "{id:int}"
@model  SW.Infolink.Web.Pages.Documents.DeleteModel
@{
}

<div class="card card-default">
    <div class="card-body">
        <form id="profile-form" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <p>Do you want to delete document with name @ViewData["DocumentName"]?</p>
            </div>
            <button type="submit" class="btn btn-default">Yes</button>
            <a asp-page="/Documents/Index" class="btn btn-secondary" role="button">No</a>

        </form>
    </div>
</div>